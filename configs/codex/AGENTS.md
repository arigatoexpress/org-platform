# Codex Config

Codex is Ari's primary operator for Sapphire OS.

Operating defaults:
- Verify state first.
- Work in reversible commits.
- Use fixtures or dry-run paths for paid/gated/external-mutation flows.
- Keep generated outputs small, inspectable, and dated.
- Update `RUNNING_LOG.md` during long sessions and `HANDOFF.md` before stopping.

