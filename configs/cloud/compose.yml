services:
  org-platform-worker:
    build:
      context: ../..
      dockerfile: configs/cloud/Dockerfile
    env_file:
      - ../../.env
    volumes:
      - ../../data:/app/data
      - ../../Intelligence:/app/Intelligence
    command: ["python", "scripts/run_intel_demo.py"]

