# Claude Project Memory

Claude is a reviewer/helper for `org-platform`, not the owner of the workstream.

Useful commands:
- `make bootstrap`
- `make test`
- `make intel-demo`
- `make crypto-demo`
- `make dashboard`

Safety reminders:
- Never paste secrets into files or chat.
- Keep Telegram, trading, and webhook sends in dry-run unless Ari explicitly authorizes live sends.
- Paid connectors should use fixtures when credentials are absent.

